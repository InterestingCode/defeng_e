<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/px90"
    android:background="@android:color/white"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/column_name_tv"
        style="@style/BaseText.black333_30px"
        android:layout_marginLeft="@dimen/px30"
        android:layout_marginRight="@dimen/px24" />

    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/px24"
        android:layout_weight="1">

        <TextView
            android:id="@+id/column_content_tv"
            style="@style/BaseText.gray999_30px"
            android:layout_gravity="right"
            android:drawablePadding="@dimen/px24"
            android:ellipsize="end"
            android:maxLines="1" />

        <ImageView
            android:id="@+id/column_icon_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:src="@drawable/icon_new_message"
            android:visibility="gone" />

        <CheckBox
            android:id="@+id/column_check_box"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:background="@drawable/selector_black_check_box"
            android:button="@null"
            android:visibility="gone" />
    </FrameLayout>

    <ImageView
        android:id="@+id/right_arrow_iv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/px24"
        android:src="@drawable/ic_right_gray_arrow" />

</LinearLayout>